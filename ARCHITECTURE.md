# 政見 ZhengJian - 完整功能架構文件

## 專案概述

**政見 political.now** 是一個 2026 台灣九合一選舉候選人政見查詢平台，提供候選人資料、政見、新聞的查詢與比較功能。

---

## 技術架構

### 前端
- **框架**: React 19 + TypeScript
- **樣式**: Tailwind CSS 4 + shadcn/ui
- **路由**: Wouter
- **狀態管理**: tRPC + React Query
- **主題**: 支援亮色/暗色主題切換

### 後端
- **框架**: Express 4 + tRPC 11
- **資料庫**: MySQL (TiDB)
- **ORM**: Drizzle ORM
- **認證**: JWT Session Cookie
- **AI 整合**: Google Gemini API (含 Search Grounding)

---

## 資料庫結構 (drizzle/schema.ts)

### 核心表格

#### 1. users - 使用者表
```typescript
- id: int (PK, auto-increment)
- openId: varchar(64) - 唯一識別碼
- name: text - 使用者名稱
- email: varchar(320)
- loginMethod: varchar(64) - 登入方式 (google/line/manus)
- role: enum('user', 'admin') - 角色
- createdAt, updatedAt, lastSignedIn: timestamp
```

#### 2. candidates - 候選人表
```typescript
- id: int (PK, auto-increment)
- name: varchar(100) - 姓名
- photoUrl: text - 照片網址
- party: varchar(50) - 政黨
- positionType: enum('mayor', 'councilor', 'township_mayor', 'representative', 'village_chief')
- county: varchar(20) - 縣市
- district: varchar(50) - 選區
- constituency: varchar(100) - 選舉區
- age: int
- education: text - 學歷
- experience: text - 經歷
- contact: text - 聯絡方式
- website: text
- socialMedia: json - 社群媒體連結
- bio: text - 簡介
- isIncumbent: boolean - 是否現任
- isActive: boolean - 是否啟用
- createdAt, updatedAt: timestamp
```

#### 3. policies - 政見表
```typescript
- id: int (PK, auto-increment)
- candidateId: int (FK -> candidates)
- categoryId: int (FK -> issue_categories)
- title: varchar(200) - 政見標題
- summary: text - 摘要
- content: text - 完整內容
- source: text - 來源
- isHighlight: boolean - 是否重點政見
- createdAt, updatedAt: timestamp
```

#### 4. candidateNews - 候選人新聞表
```typescript
- id: int (PK, auto-increment)
- candidateId: int (FK -> candidates)
- title: varchar(300) - 新聞標題
- summary: text - 摘要
- content: text - 完整內容
- sourceUrl: text - 來源網址
- sourceName: varchar(100) - 來源名稱
- imageUrl: text - 圖片網址
- topic: varchar(100) - 議題分類（用於去重）
- publishedAt: timestamp - 發布時間
- searchedAt: timestamp - 搜尋時間
- isAutoGenerated: boolean - 是否 AI 自動生成
- createdAt, updatedAt: timestamp
```

#### 5. issueCategories - 議題分類表
```typescript
- id: int (PK, auto-increment)
- name: varchar(50) - 分類名稱
- slug: varchar(50) - URL slug
- icon: varchar(50) - 圖示
- description: text - 描述
- sortOrder: int - 排序
- createdAt: timestamp
```

#### 6. comments - 評論表
```typescript
- id: int (PK, auto-increment)
- candidateId: int (FK -> candidates)
- userId: int (FK -> users)
- content: text - 評論內容
- status: enum('pending', 'approved', 'rejected')
- createdAt, updatedAt: timestamp
```

---

## API 路由結構 (server/routers.ts)

### 認證相關 (authRouter)
- `auth.me` - 取得當前使用者資訊
- `auth.logout` - 登出

### 候選人相關 (candidateRouter)
- `candidate.list` - 列出所有候選人（支援篩選）
- `candidate.getById` - 取得單一候選人詳情
- `candidate.create` - 新增候選人 (admin)
- `candidate.update` - 更新候選人 (admin)
- `candidate.delete` - 刪除候選人 (admin)
- `candidate.batchDelete` - 批次刪除候選人 (admin)
- `candidate.batchUpdate` - 批次更新候選人 (admin)
- `candidate.compare` - 比較多位候選人

### 政見相關 (policyRouter)
- `policy.getByCandidateId` - 取得候選人的所有政見
- `policy.create` - 新增政見 (admin)
- `policy.update` - 更新政見 (admin)
- `policy.delete` - 刪除政見 (admin)

### 新聞相關 (candidateNewsRouter)
- `candidateNews.getByCandidateId` - 取得候選人的所有新聞
- `candidateNews.getLatest` - 取得最新新聞
- `candidateNews.create` - 新增新聞 (admin)
- `candidateNews.delete` - 刪除新聞 (admin)

### 議題分類 (categoryRouter)
- `category.list` - 列出所有議題分類

### AI 搜尋 (aiRouter)
- `ai.searchPolicies` - AI 搜尋候選人政見
- `ai.searchNews` - AI 搜尋候選人新聞
- `ai.searchPhoto` - AI 搜尋候選人照片

### 自動更新 (autoUpdateRouter)
- `autoUpdate.searchCandidates` - 搜尋並新增候選人
- `autoUpdate.runFullUpdate` - 執行完整自動更新
- `autoUpdate.triggerScheduledUpdate` - 觸發排程更新

---

## 前端頁面結構 (client/src/pages/)

### 公開頁面
1. **Home.tsx** - 首頁
   - Hero 區塊（搜尋框、統計數據）
   - 熱門候選人展示
   - 最新動態

2. **Candidates.tsx** - 候選人列表
   - 篩選器（縣市、政黨、職位類型）
   - 候選人卡片網格

3. **CandidateDetail.tsx** - 候選人詳情
   - 基本資料
   - 政見列表
   - 新聞列表
   - 評論區

4. **News.tsx** - 新聞列表
   - 最新選舉新聞

5. **Compare.tsx** - 候選人比較
   - 多候選人政見對比

6. **Login.tsx** - 登入頁面
   - Google OAuth
   - LINE OAuth

7. **Settings.tsx** - 使用者設定

### 管理後台頁面
1. **Admin.tsx** - 管理後台首頁
   - 候選人列表（含批次操作）
   - 篩選與搜尋
   - AI 批次搜尋功能

2. **AdminCandidateEdit.tsx** - 候選人編輯
   - 基本資料編輯
   - 政見管理（新增/編輯/刪除）
   - 新聞管理（新增/刪除）
   - AI 搜尋政見/新聞功能

---

## Gemini API 整合 (server/gemini-search.ts)

### 核心功能

#### 1. searchCandidateNewsWithSources
搜尋候選人相關新聞，使用兩階段方式：
1. 使用 Search Grounding 搜尋最新新聞
2. 解析搜尋結果為結構化 JSON

**回傳格式**:
```typescript
{
  title: string;      // 新聞標題
  summary: string;    // 內容摘要
  date: string;       // 發布日期
  sourceUrl: string;  // 來源網址
  sourceName: string; // 來源名稱
}[]
```

#### 2. searchCandidatePolicies
搜尋候選人政見資料

#### 3. searchCandidatePhoto
搜尋候選人照片

#### 4. fetchAndSummarizeNews
讀取新聞連結並生成摘要

### 輔助函數
- `extractSourceName(url)` - 從 URL 提取來源名稱
- `callGeminiWithGrounding(prompt)` - 呼叫 Gemini API（含 Search Grounding）

---

## 認證系統

### OAuth 提供者
1. **Google OAuth 2.0** (`server/_core/googleOAuth.ts`)
   - 路由: `/api/auth/google`, `/api/auth/google/callback`

2. **LINE Login** (`server/_core/lineLogin.ts`)
   - 路由: `/api/auth/line`, `/api/auth/line/callback`

3. **Manus OAuth** (內建)
   - 路由: `/api/oauth/callback`

### Session 管理
- Cookie 名稱: `app_session_id`
- JWT 簽名: 使用 `JWT_SECRET`
- 有效期: 7 天

---

## 環境變數

### 系統變數
```
DATABASE_URL          - 資料庫連線字串
JWT_SECRET           - JWT 簽名密鑰
VITE_APP_ID          - Manus OAuth App ID
OAUTH_SERVER_URL     - Manus OAuth 後端 URL
VITE_OAUTH_PORTAL_URL - Manus 登入頁面 URL
```

### Google OAuth
```
GOOGLE_CLIENT_ID     - Google OAuth Client ID
GOOGLE_CLIENT_SECRET - Google OAuth Client Secret
```

### LINE Login
```
LINE_CHANNEL_ID      - LINE Channel ID
LINE_CHANNEL_SECRET  - LINE Channel Secret
```

### Gemini API
```
GEMINI_API_KEY       - Google Gemini API Key
```

### 內建 API
```
BUILT_IN_FORGE_API_URL - Manus 內建 API URL
BUILT_IN_FORGE_API_KEY - Manus 內建 API Key
```

---

## 檔案結構

```
zhengjian/
├── client/
│   ├── public/           # 靜態資源
│   │   └── images/       # 候選人照片
│   ├── src/
│   │   ├── components/   # 共用元件
│   │   │   ├── ui/       # shadcn/ui 元件
│   │   │   ├── DashboardLayout.tsx
│   │   │   ├── ThemeSwitcher.tsx
│   │   │   └── ...
│   │   ├── contexts/     # React Context
│   │   │   ├── AuthContext.tsx
│   │   │   └── ThemeContext.tsx
│   │   ├── hooks/        # 自訂 Hooks
│   │   ├── lib/          # 工具函數
│   │   │   └── trpc.ts   # tRPC 客戶端
│   │   ├── pages/        # 頁面元件
│   │   ├── App.tsx       # 路由設定
│   │   ├── main.tsx      # 入口點
│   │   └── index.css     # 全域樣式
│   └── index.html
├── server/
│   ├── _core/            # 核心框架（勿修改）
│   │   ├── context.ts    # tRPC Context
│   │   ├── env.ts        # 環境變數
│   │   ├── googleOAuth.ts
│   │   ├── lineLogin.ts
│   │   ├── llm.ts        # LLM 整合
│   │   └── ...
│   ├── db.ts             # 資料庫查詢函數
│   ├── routers.ts        # tRPC 路由
│   ├── gemini.ts         # Gemini API 基礎
│   ├── gemini-search.ts  # Gemini 搜尋功能
│   ├── autoUpdateRouter.ts # 自動更新路由
│   └── *.test.ts         # 測試檔案
├── drizzle/
│   └── schema.ts         # 資料庫 Schema
├── shared/
│   └── constants.ts      # 共用常數
├── storage/              # S3 儲存
├── todo.md               # 任務追蹤
├── package.json
└── tsconfig.json
```

---

## 常數定義 (shared/constants.ts)

### 縣市列表 (COUNTIES)
六都 + 16 縣市

### 政黨列表 (PARTIES)
- 中國國民黨、民主進步黨、台灣民眾黨
- 時代力量、台灣基進、無黨籍、其他

### 職位類型 (POSITION_TYPES)
- mayor: 縣市長
- councilor: 縣市議員
- township_mayor: 鄉鎮市長
- representative: 鄉鎮市民代表
- village_chief: 村里長

### 議題分類 (ISSUE_CATEGORIES)
交通建設、教育文化、經濟發展、社會福利、環境保護、都市規劃、醫療衛生、治安司法

---

## 待完成功能

1. **新聞搜尋優化** - Gemini Search Grounding 回傳的新聞需要更好的解析
2. **台灣地圖視覺化** - 可選功能
3. **評論審核系統** - 已有資料表，需完成 UI
4. **排程自動更新** - 已有 API，需設定 cron job

---

## 測試

執行測試:
```bash
pnpm test
```

測試檔案:
- `server/auth.logout.test.ts`
- `server/candidate.test.ts`
- `server/candidateNews.test.ts`
- `server/gemini.test.ts`
- `server/gemini.integration.test.ts`
- `server/googleOAuth.test.ts`
- `server/lineLogin.test.ts`
- `server/auto-update.test.ts`

---

## 部署

1. 建立 Checkpoint: 使用 `webdev_save_checkpoint`
2. 發布: 點擊 Management UI 的 Publish 按鈕

---

*文件更新日期: 2026/01/17*
