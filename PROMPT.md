# 政見 ZhengJian - AI 開發提示詞

## 專案背景

你正在開發「政見 political.now」- 一個 2026 台灣九合一選舉候選人政見查詢平台。這個平台提供候選人資料、政見、新聞的查詢與比較功能，幫助選民了解各候選人的政策立場。

---

## 技術架構

### 前端
- **框架**: React 19 + TypeScript
- **樣式**: Tailwind CSS 4 + shadcn/ui
- **路由**: Wouter
- **狀態管理**: tRPC + React Query
- **主題**: 支援亮色/暗色主題切換

### 後端
- **框架**: Express 4 + tRPC 11
- **資料庫**: MySQL (TiDB)
- **ORM**: Drizzle ORM
- **認證**: JWT Session Cookie (Google OAuth, LINE Login)
- **AI 整合**: Google Gemini API (gemini-3-flash-preview)

---

## 核心檔案結構

```
zhengjian/
├── client/src/
│   ├── pages/
│   │   ├── Home.tsx              # 首頁
│   │   ├── Candidates.tsx        # 候選人列表
│   │   ├── CandidateDetail.tsx   # 候選人詳情
│   │   ├── Compare.tsx           # 候選人比較
│   │   ├── News.tsx              # 新聞列表
│   │   ├── Admin.tsx             # 管理後台
│   │   └── AdminCandidateEdit.tsx # 候選人編輯
│   ├── components/
│   │   ├── ui/                   # shadcn/ui 元件
│   │   └── DashboardLayout.tsx   # 後台佈局
│   ├── lib/trpc.ts               # tRPC 客戶端
│   └── index.css                 # 全域樣式
├── server/
│   ├── routers.ts                # tRPC 路由
│   ├── db.ts                     # 資料庫查詢
│   ├── gemini.ts                 # Gemini API 基礎
│   ├── gemini-search.ts          # Gemini 搜尋功能
│   ├── scheduler.ts              # 定期排程系統
│   └── _core/                    # 核心框架（勿修改）
├── drizzle/schema.ts             # 資料庫 Schema
└── shared/constants.ts           # 共用常數
```

---

## 資料庫 Schema

### candidates 表
```typescript
{
  id: number;
  name: string;           // 姓名
  photoUrl?: string;      // 照片網址
  party?: string;         // 政黨
  positionType: 'mayor' | 'councilor' | 'township_mayor' | 'representative' | 'village_chief';
  county: string;         // 縣市
  district?: string;      // 選區
  constituency?: string;  // 選舉區
  age?: number;
  education?: string;     // 學歷
  experience?: string;    // 經歷
  contact?: string;       // 聯絡方式
  website?: string;
  socialMedia?: {         // 社群媒體
    facebook?: string;
    instagram?: string;
    youtube?: string;
    line?: string;
  };
  bio?: string;           // 簡介
  isIncumbent: boolean;   // 是否現任
  isActive: boolean;      // 是否啟用
}
```

### policies 表
```typescript
{
  id: number;
  candidateId: number;
  categoryId?: number;
  title: string;          // 政見標題
  summary?: string;       // 摘要
  content?: string;       // 完整內容
  source?: string;        // 來源
  isHighlight: boolean;   // 是否重點政見
}
```

### candidateNews 表
```typescript
{
  id: number;
  candidateId: number;
  title: string;          // 新聞標題
  summary?: string;       // 摘要
  content?: string;       // 完整內容
  sourceUrl?: string;     // 來源網址
  sourceName?: string;    // 來源名稱（中央社、聯合新聞網等）
  imageUrl?: string;      // 圖片網址
  topic?: string;         // 議題分類（用於去重）
  publishedAt?: Date;     // 發布時間
  isAutoGenerated: boolean; // 是否 AI 自動生成
}
```

---

## API 路由

### 候選人 (candidateRouter)
- `candidate.list` - 列出候選人（支援篩選）
- `candidate.getById` - 取得單一候選人
- `candidate.create` - 新增候選人 (admin)
- `candidate.update` - 更新候選人 (admin)
- `candidate.delete` - 刪除候選人 (admin)
- `candidate.compare` - 比較多位候選人
- `candidate.triggerUpdate` - 手動觸發更新 (admin)

### 政見 (policyRouter)
- `policy.getByCandidateId` - 取得候選人政見
- `policy.create` - 新增政見 (admin)
- `policy.update` - 更新政見 (admin)
- `policy.delete` - 刪除政見 (admin)

### 新聞 (candidateNewsRouter)
- `candidateNews.getByCandidateId` - 取得候選人新聞
- `candidateNews.getLatest` - 取得最新新聞
- `candidateNews.create` - 新增新聞 (admin)
- `candidateNews.delete` - 刪除新聞 (admin)

### AI 搜尋 (aiRouter)
- `ai.searchPolicies` - AI 搜尋政見
- `ai.searchNews` - AI 搜尋新聞
- `ai.searchPhoto` - AI 搜尋照片

---

## Gemini API 整合

### 模型設定
使用 `gemini-3-flash-preview` 模型

### 新聞搜尋 (searchCandidateNewsWithSources)
```typescript
// 搜尋條件
const searchPrompt = `請搜尋 ${count} 則「${candidateName}」（${county}）的正面新聞報導。

搜尋條件：
1. 優先搜尋「政績」、「爭取經費」、「服務選民」、「推動政策」、「建設成果」等正面新聞
2. 優先採用公信力高的媒體來源：中央社、聯合新聞網、自由時報、中時新聞網、ETtoday、TVBS
3. 過濾未經證實的謠言或爭議性報導
4. 避免選舉攻擊或負面新聞

請列出每則新聞的：
- 標題
- 日期
- 內容摘要（強調候選人的具體貢獻）
- 來源名稱和連結`;

// 回傳格式
interface NewsSearchResult {
  title: string;
  summary: string;
  sourceUrl: string;
  sourceName: string;
  topic: string;
  publishedDate?: string;
}
```

### 政見搜尋 (searchCandidatePolicies)
```typescript
// 搜尋候選人的政見資料
const policies = await searchCandidatePolicies(candidateName, county);
// 回傳: { title, summary, source, category }[]
```

---

## 定期排程系統 (scheduler.ts)

### 功能
- 每 6 小時自動執行更新任務
- 遍歷資料庫中的候選人
- 自動搜尋並新增新聞（正面新聞、政績）
- 自動搜尋並新增政見（當候選人政見少於 3 條時）

### 主要函數
```typescript
// 啟動排程器（伺服器啟動時自動呼叫）
startScheduler(): void

// 停止排程器
stopScheduler(): void

// 手動執行完整更新週期
runScheduledUpdate(): Promise<UpdateResult>

// 手動觸發單一候選人更新
triggerCandidateUpdate(candidateId: number): Promise<{
  success: boolean;
  newsAdded: number;
  policiesAdded: number;
  errors: string[];
}>

// 取得排程器狀態
getSchedulerStatus(): {
  isRunning: boolean;
  lastRunTime: Date | null;
  nextRunTime: Date | null;
}
```

---

## 常數定義 (shared/constants.ts)

### 縣市列表
```typescript
const COUNTIES = [
  "台北市", "新北市", "桃園市", "台中市", "台南市", "高雄市",
  "基隆市", "新竹市", "嘉義市", "新竹縣", "苗栗縣", "彰化縣",
  "南投縣", "雲林縣", "嘉義縣", "屏東縣", "宜蘭縣", "花蓮縣",
  "台東縣", "澎湖縣", "金門縣", "連江縣"
];
```

### 政黨列表
```typescript
const PARTIES = [
  "中國國民黨", "民主進步黨", "台灣民眾黨",
  "時代力量", "台灣基進", "無黨籍", "其他"
];
```

### 職位類型
```typescript
const POSITION_TYPES = {
  mayor: "縣市長",
  councilor: "縣市議員",
  township_mayor: "鄉鎮市長",
  representative: "鄉鎮市民代表",
  village_chief: "村里長"
};
```

### 議題分類
```typescript
const ISSUE_CATEGORIES = [
  { id: 1, name: "交通建設", slug: "transportation" },
  { id: 2, name: "教育文化", slug: "education" },
  { id: 3, name: "經濟發展", slug: "economy" },
  { id: 4, name: "社會福利", slug: "welfare" },
  { id: 5, name: "環境保護", slug: "environment" },
  { id: 6, name: "都市規劃", slug: "urban" },
  { id: 7, name: "醫療衛生", slug: "healthcare" },
  { id: 8, name: "治安司法", slug: "security" }
];
```

---

## 開發指南

### 新增功能流程
1. 更新 `drizzle/schema.ts` 資料庫 Schema
2. 執行 `pnpm db:push` 推送資料庫變更
3. 在 `server/db.ts` 新增查詢函數
4. 在 `server/routers.ts` 新增 tRPC 路由
5. 在前端使用 `trpc.xxx.useQuery/useMutation` 呼叫 API
6. 撰寫測試並執行 `pnpm test`

### tRPC 使用範例
```typescript
// 查詢
const { data, isLoading } = trpc.candidate.list.useQuery({
  county: "台北市",
  party: "民主進步黨"
});

// 變更
const mutation = trpc.candidate.create.useMutation({
  onSuccess: () => {
    utils.candidate.list.invalidate();
  }
});
```

### 認證檢查
```typescript
// 公開 API
publicProcedure.query(...)

// 需要登入
protectedProcedure.query(...)

// 需要管理員權限
adminProcedure.mutation(...)
```

---

## 待完成功能

1. **新聞卡片顯示優化** - 確保前端正確顯示 Gemini 回傳的結構化新聞資料
2. **台灣地圖視覺化** - 可選功能，顯示各縣市候選人分布
3. **評論審核系統** - 已有資料表，需完成 UI
4. **介面重新設計** - 可參考 ui-ux-pro-max-skill 設計規範

---

## 環境變數

```
# 資料庫
DATABASE_URL=mysql://...

# 認證
JWT_SECRET=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
LINE_CHANNEL_ID=...
LINE_CHANNEL_SECRET=...

# Gemini API
GEMINI_API_KEY=...

# Manus OAuth
VITE_APP_ID=...
OAUTH_SERVER_URL=...
VITE_OAUTH_PORTAL_URL=...
```

---

## 測試

```bash
# 執行所有測試
pnpm test

# 測試檔案
server/auth.logout.test.ts
server/candidate.test.ts
server/candidateNews.test.ts
server/gemini.test.ts
server/auto-update.test.ts
```

---

## 部署

1. 建立 Checkpoint: 使用 `webdev_save_checkpoint`
2. 發布: 點擊 Management UI 的 Publish 按鈕

---

*提示詞版本: 2026/01/17*
